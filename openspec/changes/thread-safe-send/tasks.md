> Шаги, помеченные **[совместно]**, требуют подключённого адаптера MTRF-64-USB и участия
> владельца. Конфигурация с прошлого change'а: порт **COM3**, реле SUF-1-300 (nooLite-F, ID 33347)
> на **канале 0**, датчик PT-111 на **канале 1**.

## 1. Точка отсчёта

- [x] 1.1 Убедиться, что рабочее дерево чистое и сборка зелёная: `dotnet build -c Release`
      и `dotnet test ./ThinkingHome.NooLite.Tests -c Release` (ожидается 0 предупреждений, 18 тестов)
- [x] 1.2 Добавить в `DebugConsole` режим нагрузочной отправки: N потоков параллельно шлют
      команды на заданный канал, вывод считает отправленные команды и полученные ответы
      (`Send_State` от F-блока), помечая ответы с `ResultCode` != `Success`
- [x] 1.3 **[совместно]** Снять baseline **на текущем коде, до правки**: прогнать нагрузочный
      режим и зафиксировать, воспроизводится ли порча пакетов (пропуски ответов, ошибочные
      `ResultCode`). Записать число потоков, число команд и результат — пять прогонов,
      см. `report.md`
- [x] 1.4 Если дефект не воспроизвёлся — зафиксировать это как факт и не выдавать последующий
      зелёный прогон за доказательство исправления (design → Risks) — **дефект не воспроизводится**:
      сериализация адаптера маскирует его, контрольные прогоны это доказывают

## 2. Шов над `SerialPort` — сделан и откачен

Все шаги группы были выполнены, работали и **откачены по решению владельца**: цена оказалась
выше пользы. Замеры и схема теста сохранены в `report.md` — применимы, если тестируемость
адаптера понадобится в change'е про async-приём.

- [x] ~~2.1 `internal interface ISerialDevice`~~ — откачено
- [x] ~~2.2 Реализация `SerialPortDevice` поверх `SerialPort`~~ — откачено
- [x] ~~2.3 Поле `ISerialDevice` и внутренний конструктор в `MTRFXXAdapter`~~ — откачено
- [x] ~~2.4 `InternalsVisibleTo` для тестовой сборки~~ — откачено
- [x] 2.5 Убедиться, что публичная поверхность не изменилась — **выполняется и после отката**:
      правка не добавляет и не меняет ни одного публичного члена

## 3. Исправление

- [x] 3.1 В конструкторе `MTRFXXAdapter` задать `device.WriteTimeout` конечным значением —
      принято **500 мс** (решение владельца), константа `WRITE_TIMEOUT` рядом
      с `READING_INTERVAL` / `BUFFER_SIZE`
- [x] 3.2 В `SendCommand` взять `lockObject` на время записи пакета; `BuildCommand` оставить
      вне замка — он чистый и от состояния порта не зависит
- [x] 3.3 Не заворачивать `SendCommand` в `ThreadSafeExec`: `IsOpen` не проверять, исключения
      не перехватывать — поведение по ошибкам сохраняется (design → Decision 3)
- [x] 3.4 Проверить, что публичное API не изменилось: сигнатура `SendCommand` и все обёртки
      в `MTRFXXAdapterExtensions` остались прежними
- [x] 3.5 Убедиться, что не появилось повторного входа: события `ReceiveData` /
      `ReceiveMicroclimateData` вызываются из-под `lockObject` в колбэке таймера, и обработчик,
      вызывающий `SendCommand` синхронно, попадёт на тот же замок в том же потоке
      (`lock` реентерабелен — deadlock'а нет, но факт зафиксировать в отчёте)

## 4. Юнит-тест на конкурентную запись — написан и откачен

- [x] ~~4.1 Подставная реализация `ISerialDevice` с побайтовой записью~~ — откачено
- [x] ~~4.2 Тест на 8 потоков × 40 пакетов с проверкой целостности~~ — откачено
- [x] 4.3 **Проверка теста от противного** — выполнена до отката: со снятым замком тест
      падал на проверке стартового маркера, то есть измерял именно гонку. Результат сохранён
      в `report.md` как свидетельство того, что дефект реален
- [x] ~~4.4 Тесты на `BuildCommand`~~ — откачены вместе с остальными

## 5. Сборка и тесты

- [x] 5.1 `dotnet build -c Release` — без ошибок и новых предупреждений
- [x] 5.2 `dotnet test ./ThinkingHome.NooLite.Tests -c Release` — все тесты зелёные:
      **18** существующих (разбор пакетов от правки не зависит)

## 6. Проверка на живом адаптере

- [x] 6.1 **[совместно]** Повторить прогон из 1.3 на безопасном темпе (1 поток, пауза 100 мс) —
      100 % ответов, как в baseline. Это проверка отсутствия регрессии, **не** подтверждение
      атомарности. Выполнено **до отката**, нагрузочным режимом консоли: 25 из 25, все `Success`
- [x] 6.2 **[совместно]** Обычный сценарий не сломался: `on` / `off` на канале 0 в режиме `-f`,
      блок физически переключается, приходит `Send_State`
- [x] 6.3 **[совместно]** Приём не пострадал: в режиме `listen` дождаться пакета от PT-111
      на канале 1, разбор микроклимата корректен
- [ ] 6.4 **[совместно]** Проверить поведение при отключении адаптера: выдернуть USB и вызвать
      отправку — ожидается исключение за время порядка `WriteTimeout`, а не бесконечное зависание
      — **не выполнено**: проверка требовала синхронного выдёргивания кабеля во время прогона,
      владелец её прекратил как чрезмерно сложную. Поведение `WriteTimeout` остаётся
      непроверенным на живом железе
- [x] 6.5 Сверить вывод 6.2–6.3 с baseline из прошлого change'а
      (`openspec/changes/archive/2026-08-16-update-to-net10/report.md`)

## 7. Завершение

- [x] 7.1 Отчёт: результаты 1.3, 4.3, 5.1–5.2, 6.1–6.4; явно указать, что дефект на железе
      не воспроизводится, а юнит-тест откачен — автоматической проверки атомарности нет
- [x] 7.2 Решить с владельцем, коммитить ли нагрузочный режим `DebugConsole` — **откачен**
      вместе с остальным инструментарием проверки
